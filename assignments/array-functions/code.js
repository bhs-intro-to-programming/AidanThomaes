const countTens = (numbers) => {
  if (numbers.length > 0) {
    for (let x = 0; x < numbers.length; x++ ) {  
      if (numbers[x]===10) {
       
      } 
    }
  } else {
    return 0
  } 
} 

const sum = (numbers) => {
  if (numbers.length > 0){
    for (let x = 0; x < numbers.length; x++) {
      
    }
  } else {
    return 0
  }
}