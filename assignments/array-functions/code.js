let numTens = 0
const countTens = (numbers) => {
  if (numbers.length > 0) {
    for (let x = 0; x < numbers.length; x++ ) {  
      if (numbers[x]===10) {
       
      } 
    }
  } else {
    return 0
  } 
} 

const sum = (numbers) => {
  if (numbers.length > 0){
    for (let x = 0; x < numbers.length; x++) {
      return numbers[0] + numbers[1]
    }
  } else {
    return 0
  }
}